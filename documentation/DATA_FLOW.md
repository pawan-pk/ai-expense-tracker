# 🔄 Data Flow Documentation

Visual guide to how data flows through the AI Expense Tracker.

## Overview

```
User Input → Mobile App → Backend API → AI Service → Database → Response → Mobile App → User
```

## Detailed Flow Diagrams

### 1. Adding an Expense

```
┌─────────────────────────────────────────────────────────────────────┐
│ STEP 1: User Input                                                  │
├─────────────────────────────────────────────────────────────────────┤
│ User types: "spent 850 on lunch at Taj"                            │
│ Taps "Add" button                                                   │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ STEP 2: Mobile App (App.tsx)                                       │
├─────────────────────────────────────────────────────────────────────┤
│ handleAddExpense() called                                           │
│ setLoading(true)                                                    │
│ Calls: addExpense(input)                                            │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ STEP 3: API Service (src/services/api.ts)                          │
├─────────────────────────────────────────────────────────────────────┤
│ POST http://localhost:3000/api/expenses                             │
│ Headers: { "Content-Type": "application/json" }                    │
│ Body: { "input": "spent 850 on lunch at Taj" }                     │
│ Timeout: 10 seconds                                                 │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ STEP 4: Backend Route (src/routes/expenses.ts)                     │
├─────────────────────────────────────────────────────────────────────┤
│ Receives POST request                                               │
│ Validates input (not empty)                                         │
│ Calls: parseExpense(input)                                          │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ STEP 5: AI Service (src/services/aiService.ts)                     │
├─────────────────────────────────────────────────────────────────────┤
│ Prepares AI request:                                                │
│   - System prompt (parsing rules)                                   │
│   - User message (the input text)                                   │
│   - Model: llama-3.1-70b-versatile                                  │
│                                                                     │
│ POST https://api.groq.com/openai/v1/chat/completions               │
│ Headers: { "Authorization": "Bearer API_KEY" }                     │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ STEP 6: AI API Response                                            │
├─────────────────────────────────────────────────────────────────────┤
│ Returns JSON:                                                       │
│ {                                                                   │
│   "amount": 850,                                                    │
│   "currency": "INR",                                                │
│   "category": "Food & Dining",                                      │
│   "description": "Lunch at Taj",                                    │
│   "merchant": "Taj"                                                 │
│ }                                                                   │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ STEP 7: AI Service Validation                                      │
├─────────────────────────────────────────────────────────────────────┤
│ Parses JSON response                                                │
│ Validates fields exist                                              │
│ Sets defaults if needed                                             │
│ Returns ParsedExpense object                                        │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ STEP 8: Backend Route (continued)                                  │
├─────────────────────────────────────────────────────────────────────┤
│ Receives parsed expense                                             │
│ Calls: createExpense(parsed)                                        │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ STEP 9: Database (src/database/db.ts)                              │
├─────────────────────────────────────────────────────────────────────┤
│ INSERT INTO expenses (                                              │
│   amount, currency, category,                                       │
│   description, merchant, original_input                             │
│ ) VALUES (850, 'INR', 'Food & Dining', ...)                        │
│                                                                     │
│ Returns: Expense with ID and created_at                             │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ STEP 10: Backend Response                                          │
├─────────────────────────────────────────────────────────────────────┤
│ Status: 201 Created                                                 │
│ Body: {                                                             │
│   "success": true,                                                  │
│   "expense": {                                                      │
│     "id": 1,                                                        │
│     "amount": 850,                                                  │
│     "currency": "INR",                                              │
│     "category": "Food & Dining",                                    │
│     "description": "Lunch at Taj",                                  │
│     "merchant": "Taj",                                              │
│     "original_input": "spent 850 on lunch at Taj",                 │
│     "created_at": "2025-01-20T10:30:00Z"                           │
│   }                                                                 │
│ }                                                                   │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ STEP 11: API Service (continued)                                   │
├─────────────────────────────────────────────────────────────────────┤
│ Receives response                                                   │
│ Checks response.ok                                                  │
│ Parses JSON                                                         │
│ Returns expense object                                              │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ STEP 12: Mobile App (continued)                                    │
├─────────────────────────────────────────────────────────────────────┤
│ Receives expense from API                                           │
│ Updates state:                                                      │
│   - setExpenses([expense, ...expenses])                             │
│   - setInput('')                                                    │
│   - setSuccessExpense(expense)                                      │
│   - setLoading(false)                                               │
│                                                                     │
│ Shows success card for 3 seconds                                    │
│ Expense appears in list                                             │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ STEP 13: User Sees Result                                          │
├─────────────────────────────────────────────────────────────────────┤
│ ✅ Added Successfully!                                              │
│ Amount: ₹850                                                        │
│ Category: 🍔 Food & Dining                                          │
│ Description: Lunch at Taj                                           │
│ Merchant: Taj                                                       │
└─────────────────────────────────────────────────────────────────────┘
```

### 2. Loading Expenses

```
┌─────────────────────────────────────────────────────────────────────┐
│ User opens app or pulls to refresh                                 │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Mobile App: loadExpenses() or onRefresh()                          │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ API Service: GET http://localhost:3000/api/expenses                │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Backend Route: GET /api/expenses                                   │
│ Calls: getAllExpenses()                                             │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Database: SELECT * FROM expenses ORDER BY created_at DESC          │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Backend Response:                                                   │
│ {                                                                   │
│   "success": true,                                                  │
│   "expenses": [...]                                                 │
│ }                                                                   │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Mobile App: setExpenses(data)                                      │
│ FlatList renders expense items                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 3. Deleting an Expense

```
┌─────────────────────────────────────────────────────────────────────┐
│ User taps 🗑️ button                                                 │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Mobile App: Alert.alert() shows confirmation                       │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ User confirms deletion                                              │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Mobile App: handleDeleteExpense(id)                                │
│ setDeletingId(id) - shows loading                                   │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ API Service: DELETE http://localhost:3000/api/expenses/1           │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Backend Route: DELETE /api/expenses/:id                            │
│ Validates ID is number                                              │
│ Calls: deleteExpense(id)                                            │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Database: DELETE FROM expenses WHERE id = 1                        │
│ Returns: number of rows deleted                                     │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Backend Response:                                                   │
│ {                                                                   │
│   "success": true,                                                  │
│   "message": "Expense deleted successfully"                         │
│ }                                                                   │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Mobile App:                                                         │
│ setExpenses(expenses.filter(e => e.id !== id))                     │
│ setDeletingId(null)                                                 │
│ Expense removed from list                                           │
└─────────────────────────────────────────────────────────────────────┘
```

## State Management

### Mobile App State

```typescript
// App.tsx state variables
const [input, setInput] = useState(""); // Text input value
const [expenses, setExpenses] = useState<Expense[]>([]); // List of expenses
const [loading, setLoading] = useState(false); // Add button loading
const [refreshing, setRefreshing] = useState(false); // Pull-to-refresh
const [successExpense, setSuccessExpense] = useState<Expense | null>(null); // Success card
const [deletingId, setDeletingId] = useState<number | null>(null); // Delete loading
```

### Backend State

```typescript
// Database connection (persistent)
const db = new Database("expenses.db");

// No in-memory state - all data in SQLite
```

## Error Handling Flow

### Mobile App Error

```
API call fails
    ↓
catch (error)
    ↓
Alert.alert('Error', error.message)
    ↓
setLoading(false) / setDeletingId(null)
    ↓
User sees error message
```

### Backend Error

```
Error occurs (AI API fails, DB error, etc.)
    ↓
catch (error)
    ↓
console.error(error)
    ↓
res.status(500).json({ success: false, error: message })
    ↓
Mobile app receives error response
    ↓
Shows Alert to user
```

### AI Parsing Error

```
AI returns invalid JSON or error
    ↓
aiService catches error
    ↓
Returns { amount: null, error: "message" }
    ↓
Backend route checks parsed.error
    ↓
Returns 400 with error message
    ↓
Mobile app shows error Alert
```

## Data Transformations

### Input Text → Structured Data

```
"spent 850 on lunch at Taj"
    ↓ AI Processing
{
  amount: 850,
  currency: "INR",
  category: "Food & Dining",
  description: "Lunch at Taj",
  merchant: "Taj"
}
    ↓ Database Storage
{
  id: 1,
  amount: 850,
  currency: "INR",
  category: "Food & Dining",
  description: "Lunch at Taj",
  merchant: "Taj",
  original_input: "spent 850 on lunch at Taj",
  created_at: "2025-01-20T10:30:00Z"
}
    ↓ Mobile Display
┌─────────────────────────────────┐
│ 🍔 Food & Dining      ₹850.00  │
│    Lunch at Taj                 │
│    at Taj                       │
│    2 minutes ago       🗑️       │
└─────────────────────────────────┘
```

## Timing

Typical request times:

```
User Input → Mobile App:           < 1ms
Mobile App → Backend API:          10-50ms (network)
Backend → AI API:                  500-2000ms (AI processing)
AI API → Backend:                  10-50ms (network)
Backend → Database:                < 10ms
Database → Backend:                < 10ms
Backend → Mobile App:              10-50ms (network)
Mobile App → User Display:         < 1ms

Total: ~1-2 seconds for adding expense
```

## Network Requests

### Add Expense

```http
POST /api/expenses HTTP/1.1
Host: localhost:3000
Content-Type: application/json

{
  "input": "spent 850 on lunch at Taj"
}
```

### Get Expenses

```http
GET /api/expenses HTTP/1.1
Host: localhost:3000
```

### Delete Expense

```http
DELETE /api/expenses/1 HTTP/1.1
Host: localhost:3000
```

## Database Operations

### Insert

```sql
INSERT INTO expenses (
  amount, currency, category,
  description, merchant, original_input
) VALUES (
  850, 'INR', 'Food & Dining',
  'Lunch at Taj', 'Taj', 'spent 850 on lunch at Taj'
);
```

### Select

```sql
SELECT * FROM expenses
ORDER BY created_at DESC;
```

### Delete

```sql
DELETE FROM expenses
WHERE id = 1;
```

## Component Interaction

```
App.tsx
├── Header (static)
├── Input Section
│   ├── TextInput (controlled by input state)
│   └── Add Button (calls handleAddExpense)
├── Success Card (conditional on successExpense)
├── List Header (static)
└── FlatList
    ├── Pull to Refresh (calls onRefresh)
    └── Expense Items (map over expenses)
        └── Delete Button (calls handleDeleteExpense)
```

## API Service Flow

```typescript
// api.ts structure
export async function addExpense(input: string): Promise<Expense> {
	// 1. Create abort controller for timeout
	// 2. Make fetch request
	// 3. Handle timeout
	// 4. Parse response
	// 5. Check for errors
	// 6. Return expense or throw error
}
```

---

This data flow documentation helps you understand how information moves through the system. Use it when debugging or adding new features!
